#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char grammer[] =  "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", grammer);

	return (0);
}
