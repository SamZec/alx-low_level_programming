#include <stdio.h>

/**
 * main - Entry piont
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	/*initailize puzzel to hold string*/
	char puzzel[] = "\"Programming is like building a multilingual puzzle";

	/*call puzzel with puts to print the string*/
	puts(puzzel);

	/*return 0 if success*/
	return (0);
}
