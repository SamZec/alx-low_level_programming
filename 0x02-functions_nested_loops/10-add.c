#include "main.h"

/**
 * add - adds two integers returns the results
 * @a: takes one integer
 * @b: takes the other integer
 *
 * Return: results of a + b
 */
int add(int a, int b)
{
	return (a + b);
}
